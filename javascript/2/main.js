// 데이터 = 값(value) + 값의유형(type) 
// 자료형은 값이 가질 수 있는 여러가지 유형을 구분한 개념 
// 자료형은 원시타입(숫자,문자열,논리(boolean), null, undefined, symbol)과 참조타입(배열, 객체, 함수)으로 구분한다.

// 원시타입 자료형
let number = 1;
let str = "abc";
let bool = true;
let undi = undefined;
let nul = null;
let symbol = Symbol();


console.log(typeof(123) )

console.log()


//참조타입 자료형

let array = []; //배열(object)
let obj = {};   //객체(object)
let func = function(){};    //함수(function)

console.log(typeof([]));